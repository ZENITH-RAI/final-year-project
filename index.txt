<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Used Car Price Predictor</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap');
        
        :root {
            --primary: #4F46E5;
            --bg-color: #F3F4F6;
            --card-bg: #FFFFFF;
            --text-dark: #1F2937;
            --text-light: #6B7280;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-dark);
            margin: 0;
            padding: 2rem;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 2rem;
        }

        h1 { margin-bottom: 0.5rem; color: var(--text-dark); }
        .subtitle { color: var(--text-light); }

        /* Metrics Section */
        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .metric-card {
            background: var(--card-bg);
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            text-align: center;
            border-bottom: 4px solid var(--primary);
        }

        .metric-title { font-size: 0.875rem; color: var(--text-light); font-weight: 600; text-transform: uppercase;}
        .metric-value { font-size: 1.5rem; font-weight: 700; margin-top: 0.5rem; }

        /* Form Section */
        .form-container {
            background: var(--card-bg);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: 600;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
        }

        input, select {
            padding: 0.75rem;
            border: 1px solid #D1D5DB;
            border-radius: 6px;
            font-size: 1rem;
            transition: border-color 0.2s;
        }

        input:focus, select:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.2);
        }

        .submit-btn {
            background-color: var(--primary);
            color: white;
            border: none;
            padding: 1rem 2rem;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            width: 100%;
            margin-top: 2rem;
            transition: background-color 0.2s;
        }

        .submit-btn:hover { background-color: #4338CA; }

        /* Result Section */
        .result-box {
            margin-top: 2rem;
            padding: 2rem;
            background: linear-gradient(135deg, #4F46E5 0%, #3B82F6 100%);
            color: white;
            text-align: center;
            border-radius: 12px;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.2);
            animation: fadeIn 0.5s ease-in-out;
        }

        .result-title { font-size: 1.25rem; opacity: 0.9; }
        .result-price { font-size: 3rem; font-weight: 700; margin-top: 0.5rem; }

        .error-box { background: #FEE2E2; color: #991B1B; padding: 1rem; border-radius: 8px; margin-top: 1rem; text-align: center;}

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

<div class="container">
    <div class="header">
        <h1>Used Car Price Predictor</h1>
        <p class="subtitle">Powered by Custom Mini-Batch SGD Algorithm</p>
    </div>

    {% if metrics %}
    <div class="metrics-grid">
        <div class="metric-card">
            <div class="metric-title">R-Squared (R2)</div>
            <div class="metric-value">{{ metrics.R2 }}</div>
        </div>
        <div class="metric-card">
            <div class="metric-title">Mean Abs Error (MAE)</div>
            <div class="metric-value">{{ metrics.MAE }}</div>
        </div>
        <div class="metric-card">
            <div class="metric-title">Mean Sq Error (MSE)</div>
            <div class="metric-value">{{ metrics.MSE }}</div>
        </div>
        <div class="metric-card">
            <div class="metric-title">MAPE</div>
            <div class="metric-value">{{ metrics.MAPE }}</div>
        </div>
    </div>
    {% endif %}

    <div class="form-container">
        <form action="/predict" method="POST">
            <div class="form-grid">
                
                <div class="form-group">
                    <label>Brand</label>
                    <input type="text" name="brand" value="Maruti" required>
                </div>
                
                <div class="form-group">
                    <label>Model</label>
                    <input type="text" name="model" value="Other" required>
                </div>

                <div class="form-group">
                    <label>Manufacturing Year</label>
                    <input type="number" name="year" value="2019" required>
                </div>

                <div class="form-group">
                    <label>Kilometers Driven</label>
                    <input type="number" name="km_driven" value="10000" step="1" required>
                </div>

                <div class="form-group">
                    <label>Fuel Type</label>
                    <select name="fuel">
                        <option value="CNG">CNG</option>
                        <option value="Petrol">Petrol</option>
                        <option value="Diesel">Diesel</option>
                        <option value="LPG">LPG</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Transmission</label>
                    <select name="transmission">
                        <option value="Manual">Manual</option>
                        <option value="Automatic">Automatic</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Seller Type</label>
                    <select name="seller_type">
                        <option value="Individual">Individual</option>
                        <option value="Dealer">Dealer</option>
                        <option value="Trustmark Dealer">Trustmark Dealer</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Owner</label>
                    <select name="owner">
                        <option value="First Owner">First Owner</option>
                        <option value="Second Owner" selected>Second Owner</option>
                        <option value="Third Owner">Third Owner</option>
                        <option value="Fourth & Above Owner">Fourth & Above Owner</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Mileage (kmpl)</label>
                    <input type="number" name="mileage" value="20.0" step="0.1" required>
                </div>

                <div class="form-group">
                    <label>Engine (CC)</label>
                    <input type="number" name="engine" value="796" step="0.1" required>
                </div>

                <div class="form-group">
                    <label>Max Power (bhp)</label>
                    <input type="number" name="max_power" value="40.3" step="0.1" required>
                </div>

                <div class="form-group">
                    <label>Seats</label>
                    <input type="number" name="seats" value="4" step="1" required>
                </div>
            </div>

            <button type="submit" class="submit-btn">Predict Fair Market Value</button>
        </form>

        {% if error %}
        <div class="error-box">
            <strong>Error:</strong> {{ error }}
        </div>
        {% endif %}

        {% if price %}
        <div class="result-box">
            <div class="result-title">Estimated Vehicle Price</div>
            <div class="result-price">{{ price }}</div>
        </div>
        {% endif %}
    </div>
</div>

</body>
</html>
